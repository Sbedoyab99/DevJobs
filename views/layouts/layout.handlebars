<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;700;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/app.css">
  <link rel="stylesheet" type="text/css" href="https://unpkg.com/trix@2.0.0/dist/trix.css">
  <title>DevJobs | {{nombrePagina}}</title>
</head>
<body>
  <div class="nombre-sitio contenedor">
    <h1>
      <a href="/">DevJobs</a>
    </h1>
    {{#if barra}}
      <div class="buscador">
        <form action="#">
          <input type="text" class="buscar">
          <input type="submit" value="buscar">
        </form>
      </div>
    {{/if}}
  </div>
  <div class="header site-header contenedor separador">
    <h2>{{nombrePagina}}</h2>
    {{#if tagline}}
      <p class="tagline">{{tagline}}</p>
    {{/if}}
    {{#if boton}}
      <a href="/vacantes/nueva" class="btn btn-azul">Publicar Nueva Vacante</a>
    {{/if}}
  </div>
  <div class="contenido-principal contenedor">
    {{#if mensajes}}
      <div class="alertas">
        {{#mostrarAlertas mensajes}}
        {{/mostrarAlertas}}
      </div>
    {{/if}}
    {{{body}}}
  </div>
</body>
<script src="/dist/alertas.js"></script>
</html>